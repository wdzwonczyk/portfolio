---
export interface Props {
	tag: string;
}

const { tag } = Astro.props;
---

<span class="px-2 py-2 text-xs font-semibold bg-white/50 backdrop-blur-lg absolute left-4 bottom-4 rounded-lg z-[999]">{tag}</span>
